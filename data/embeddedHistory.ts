import { HistoryItem } from '../types';

// Paste the exported JSON array between the brackets below to ship a default
// history snapshot with the source code. On a fresh browser/server the
// History page shows a "Restore snapshot" button that imports these items.
export const EMBEDDED_HISTORY: HistoryItem[] = [];
